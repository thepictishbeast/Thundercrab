# ThunderCrab Android — Gradle properties (BUILD group). AVP-2: UNVERIFIED — UNSAFE.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# NOTE: org.gradle.configuration-cache is deliberately OFF. The manual UniFFI
# Exec tasks read android.ndkDirectory at configuration time and are not
# configuration-cache-safe as written. Re-enabling it is a documented follow-up.
